{
  "mcpServers": {
    "intervals-icu": {
      "command": "node",
      "args": ["src/index.mjs"],
      "env": {
        "INTERVALS_API_KEY": "${INTERVALS_API_KEY}",
        "INTERVALS_ATHLETE_ID": "${INTERVALS_ATHLETE_ID}"
      }
    }
  }
}
